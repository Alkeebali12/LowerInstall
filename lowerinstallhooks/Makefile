{
    Filter = {
        Bundles = (
            "com.apple.AppStore"
        );
        Executables = (
            appstored,
            itunesstored,
            storekitd,
            installd
        );
    };
}
